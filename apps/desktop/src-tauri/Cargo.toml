[package]
name = "nembrix"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

[lib]
name = "nembrix_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"
tauri-plugin-dialog = "2"
tauri-plugin-fs = "2"
tauri-plugin-os = "2"
# Auto-updater: checks a signed manifest on the GitHub release, downloads
# and installs the new bundle. `process` is its companion — `relaunch()`
# restarts the app after the install is staged.
tauri-plugin-updater = "2"
tauri-plugin-process = "2"
tauri-specta = { version = "2.0.0-rc", features = ["typescript"] }
specta.workspace = true
specta-typescript = "0.0.7"

db-core.workspace = true
db-postgres.workspace = true
db-mongo.workspace = true
ssh-tunnel.workspace = true
secrets.workspace = true
sql-format.workspace = true
sqlx.workspace = true

tokio = { workspace = true, features = ["full"] }
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
async-trait.workspace = true
uuid.workspace = true
chrono.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
anyhow.workspace = true
